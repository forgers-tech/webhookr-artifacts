# Engineering Artifact Index

Newest first. One line per artifact. Follows the same storage convention as `marketing/` (see `../CLAUDE.md`).

## 2026-07-15 — terraform-cd-pipeline (manual, artifact-reconciliation)
- [adr-terraform-cd-pipeline v1](2026-07-15-terraform-cd-pipeline/2026-07-15-adr-terraform-cd-pipeline-v1.md) — **Approved. ADR-008.** Records the *why* of applying Terraform from **GitHub Actions** as a phased, human-gated CD pipeline (in implementation: [terraform#107](https://github.com/forgers-tech/terraform/pull/107)/[#108](https://github.com/forgers-tech/terraform/pull/108)/[#109](https://github.com/forgers-tech/terraform/pull/109)). Realizes the [terraform-auto-apply council v2](2026-07-14-terraform-auto-apply-council/2026-07-14-council-record-v2.md) (D-1..D-14, BR-015) + [feature spec v2](2026-07-14-terraform-auto-apply/2026-07-14-feature-spec-terraform-auto-apply-v2.md). **Supersedes** `terraform/CLAUDE.md`'s "there is no CD pipeline" posture and **extends [ADR-003](2026-07-05-infra-security-matrix/2026-07-05-adr-infra-security-matrix-v1.md)** with a new accepted CI trust-boundary crossing (**T-11 — CI Terraform apply**: WIF + ephemeral GitHub App token + SOPS/age, SHA-pinned actions, reviewer-gated prod Environment, no plaintext plan/state; residual = crown-jewel concentration + single-maintainer self-approval + no true rollback). End-state = on-merge apply behind an Environment reviewer gate; Phase-1 mono-root dispatch-only; firebase-dev canary; mono-root apply blocked by the **BR-015** gate. The operational security-matrix row (gitops) must be added on rollout. This ADR is rationale only; the decisions live in the council record.

## 2026-07-15 — future-council-command (manual)
- [adr-future-council-command v1](2026-07-15-future-council-command/2026-07-15-adr-future-council-command-v1.md) — **Approved. ADR-008.** Records the *why* of adding **`/future-council`** as a **foresight profile of `/council`**, shipped in `forgers-tech/skills` v1.8.0 ([PR #27](https://github.com/forgers-tech/skills/pull/27)). Distinct job from the lifecycle `/council`: not "refine one User Story card" but "discover the capabilities becoming **inevitable** for a category over a multi-year horizon (default 2026–2030)" — so it swaps the four-item card gate for a lighter foresight bar (strategic question + scope + horizon), runs a fixed **14-specialist futures roster** (adds Industry Futurist, AI Systems Architect, Product Strategist, VC Partner, Blue Ocean Strategist, permanent Red Team + Technology Historian, and a Technology Economist owning a per-finalist Timing Assessment) over **five rounds** (Independent → Cross-Review → Emergent → First-Principles → Prioritization), and produces **four separate audience-specific artifacts** (Council Record + Decision Ledger + Final Synthesis · Executive Summary · Action Plan · Knowledge Base) — never merged. Reuses ADR-006's orchestrate-never-simulate engine and ADR-007's persistence discipline **unchanged** (profile, not fork); bakes in file-first + resume-don't-restart resilience proven necessary by the live run. **Standalone — not a delivery-lifecycle stage** (its Ledger *can* feed `/upstream` when a "Build Now" decision is chosen, but the run commits to no implementation). Alternatives rejected: a `--foresight` mode on `/council` (dilutes the hard-gate identity), re-pasting the bespoke prompt (not repeatable), a full re-implementation (guarantees drift). Validated by the [2026-07-15 Future Council run](2026-07-15-future-council-webhookr-2030/2026-07-15-council-record-v1.md) executed via `/council` before the command existed. Implementation lives in the `skills` repo; this ADR is rationale only

## 2026-07-15 — future-council-webhookr-2030 (/council, foresight)
- [council-record v1](2026-07-15-future-council-webhookr-2030/2026-07-15-council-record-v1.md) — **Approved. Strategy decision record (not a spec).** Critical-depth foresight `/council` ("Future Council — Webhookr 2030"): 14 specialists (incl. Technology Historian + Technology Economist), 5 rounds, 33 independent agent executions; the Chair authored no specialist output. **Verdict: build the 2026 reliability floor in its end-state shape ("the shaped floor" — the audit remediation and the 2030 moat are the same line items): 5 seams + a CTO-capped 24-eng-day amendment set (receipts+cursor-transitions as the hash-chained spine, record-envelope schema, dual-stack signing + OSS verifiers, per-tenant-DEK crypto-shredding retention + erasure certificates, policy-object quotas on an extensible principal model, registry-shaped contract package, lease-shaped DLQ, telemetry fields), under four design laws (AI-at-design-time only; never meter meaning; control/data separability; the free-field test), starting three non-retroactive clocks (evidence history, reliability telemetry, contract corpus).** Nine conflicts resolved (self-healing → Diagnosed Delivery/Dependabot form; agent surface fused-only with wake→drain→query and 2028 demand gates; substrate = phased payload-by-reference, re-founding rejected; identity delivery = own-the-build/fast-follow-the-standard, $0 pre-2028; registry private+drift with 2028 tripwires; provenance = two SKUs one chain, v1 Team-tier 2026; customer WASM 2028 triple-gated; witnessing mechanism-in-floor/publication-at-v1-GA; Red-Team's 55% T1 dissent recorded, not forced). Emergent layer all FUSED, none standalone (clearinghouse seeds, org-memory query door, JIT entitlements as P2 metering, assurance exhaust, split-plane as design law); E-9 escrow killed. Deliverables: Top-10 inevitable capabilities, Top-5 blue-ocean, Top-3 redefining ideas, per-finalist timing windows (portfolio law: a floor slip *shrinks* every window), product arc P1 flight-recorder → P2 Agent Inbox → P3 Trust Plane, fundraising vocabulary firewall ("agent accountability", never "provenance ledger"). Key code-audit corrections folded in: timeline is cascade-deletable today, single global DEK, Redis-SPOF premise stale. 6 US, 14 D-#, full A/F/Q/R/X registers. `⟶ /upstream <record path>` scoped to D-1 (the shaped floor)

## 2026-07-14 — terraform-auto-apply (/upstream → /gatekeeper)
- [gatekeeper-review v1](2026-07-14-terraform-auto-apply/2026-07-14-gatekeeper-review-v1.md) — **🟡 Approved with Recommendations — 93/100.** Gatekeeper review of the Terraform-auto-apply Feature Spec. **Traceability Audit clean on all five checks** — D-1..D-14 all expanded, no invented requirement, no decision drift, Q-1..Q-4 resolved in the *ledger* (Rev-2) not silently by the spec, no revived X-#. No blocking issues; Phase 0/1 (bootstrap root + reusable `_tf-apply.yml` + firebase-dev canary) can start now. Scores: Product 24, Business Rules 23, Architecture 23, Tech Leadership 23. Important risks: **IR-1** the load-bearing D-4 age-sealed hand-off is first exercised at firebase-**prod** (firebase-dev is ungated) — ensure the decrypt/gate path is tested before mono-root; **IR-2** pin the binary-`tfplan` sealing to `age -r`/`age -d` (not `sops`, which is for the structured secret bundle); **IR-3** the out-of-band bootstrap (WIF + App→SOPS + Environments) is the predecessor gating Steps 5–7 — move the App key from `.env` into SOPS+age; **IR-4** add an early `python3`/`curl` preflight (A-1). Mono-root apply stays governed by the **BR-015** blocking gate. Next: `/downstream` (Phase 0/1).
- [feature-spec-terraform-auto-apply v2](2026-07-14-terraform-auto-apply/2026-07-14-feature-spec-terraform-auto-apply-v2.md) — **current. Draft + GK refinements.** v1 + the Gatekeeper v1 Important Risks folded in (no decision change): **IR-1** gated (two-job `age`-sealed) vs ungated firebase-dev (single-job) apply flows made explicit + a mandatory firebase-prod decrypt/gate test before the mono-root; **IR-2** binary `tfplan` sealed with `age -r`/`age -d` (sops reserved for the secret bundle); **IR-3** out-of-band bootstrap is a hard predecessor of Steps 5–7 + App key moves `.env`→SOPS+age; **IR-4** early `python3`/`curl` preflight. Ready for `/downstream` (Phase 0/1).
- [feature-spec-terraform-auto-apply v1](2026-07-14-terraform-auto-apply/2026-07-14-feature-spec-terraform-auto-apply-v1.md) — **superseded by v2.** Authors the [terraform-auto-apply council v2](2026-07-14-terraform-auto-apply-council/2026-07-14-council-record-v2.md) into an implementation-ready spec — every section cites `⟶ D-#`, no re-discovery. Build a **new sibling `terraform-apply.yml`** (CI unchanged) = reusable `_tf-apply.yml` per `(root × env)`: WIF for GCS + **ephemeral GitHub App token** for admin:org + **SOPS+age** third-party secrets on GitHub-hosted ephemeral runners; **age-sealed saved-plan hand-off** on the squash SHA (plan job → encrypted artifact → Environment-gated apply job decrypts + `apply tfplan`) so applied==approved holds despite the job-boundary gate; per-prefix `concurrency` + in-CI force-unlock; no-destroy + clean-baseline HARD guards (diff-scoping advisory); `TF_APPLY_ENABLED` disable + `make apply` break-glass. 15 BR-# (each `⟶ D-#`), most §-sections N/A (no app/API/DB/events). Rollout ladder: firebase-dev on-merge (fast-path) → firebase-prod gated → **mono-root plan-only-to-green** (risk canary) → mono-root gated apply. **BR-015 blocking gate** on the mono-root apply (P0.1 App-token + drift=0 + reviewer-approved squash-SHA plan). Prep done: firebase tfvars committed ([terraform#105](https://github.com/forgers-tech/terraform/pull/105)), mono-root drift zeroed (`No changes`). Open `TODO:` = A-1 (runner python3/curl), A-3 exact App perms, A-5 provider OIDC recheck. Estimate **GG**. Next: `/gatekeeper`

## 2026-07-14 — terraform-auto-apply-council (/council)
- [council-record v2](2026-07-14-terraform-auto-apply-council/2026-07-14-council-record-v2.md) — **current. Approved + operator resolutions.** v1 + Rev-2 deltas after the operator answered the open questions (Chair validated Q-1/A-3 against the repo; no `D-#` invalidated). **Q-1 RESOLVED** (validated: the GH Environment gate pauses at the **job boundary**, not mid-job — per `apply-toggles.yml`; the *hard* applied==approved is preserved via a **two-job age-sealed `tfplan` hand-off**, reusing the SOPS age key). **A-3 RESOLVED** (viable: `integrations/github ~>6.0` `app_auth` + `create-github-app-token`). **Q-2 → SOPS+age** (single Environment secret, also seals the plan). **Q-3 → accepted** single-maintainer self-approval (R-8). **Q-4 → firebase-dev fast-path** (ungated). **NF-C corrected** — the `GITOPS_DEPLOY_PAT` shadow-override is a *real* 2026-07-01 operational incident (project memory `google-auth.md`), just not in-repo. Next: `/upstream <v2 path>`.
- [council-record v1](2026-07-14-terraform-auto-apply-council/2026-07-14-council-record-v1.md) — **superseded by v2.** Original Critical-depth `/council` on **automatically applying Terraform from GitHub Actions**. 7 independent Round-1 agents + 5 Round-2 negotiators + 3 challenge pairs; the Chair authored none. **Headline:** the story reverses an explicit recorded decision (`terraform/CLAUDE.md` "There is no CD pipeline that applies Terraform") → needs a superseding ADR + Security-Matrix row on implementation. Verdict: **build a phased, human-gated GitHub-Actions delivery pipeline.** End-state = **on-merge apply behind a GitHub Environment required-reviewer gate** (mirrors the `apply-toggles.yml` precedent; the reviewer gate *is* the card's "required approval condition" under `required_approving_review_count=0`). Four conflicts resolved: **F-1** trigger — dispatch-first for the mono-root (time-boxed ACCEPTED-RISK), auto-on-merge only for firebase-dev, mono-root last; **F-2** no credentialed plan on PRs for admin:org roots (`data.external` runs admin:org curl at *plan* time) — plan post-merge, reviewed at the gate; **F-3** creds = **WIF for GCS + ephemeral GitHub App token for admin:org** (retires the static PAT) + Environment-scoped third-party statics on GitHub-hosted ephemeral runners (persistent self-hosted runner **rejected**); **F-4** owned **BLOCKING GATE** — the mono-root's first *apply* is authorized only when (1) the P0.1 admin:org lateral path is closed (App token + out-of-band pipeline creds), (2) a zero-drift baseline is proven by a **mono-root plan-only-to-green** stage, (3) a same-job **saved-plan** apply of the exact post-squash commit is approved at the Environment gate. Challenges reclassified the diff-scoping + RCE guards as **advisory** (mono-root safety rests on human-in-the-loop + hard controls: same-runner saved-plan apply, no-destroy guard, clean-baseline gate, provider pinning) and added the plan-only risk-canary. New facts folded in: firebase `*.tfvars` untracked → **commit them**; squash-only merge → applied==approved is **content-equality on the squash SHA**; the `GITOPS_DEPLOY_PAT` shadow-override memory is **unverified** (not found in-repo). 10 US, 14 D-#, full A/F/Q/R/X registers. Open **Q-1** (can a GH Environment approval hold mid-job to keep the `tfplan`?) decides whether D-4's applied==approved is tool-enforced or human-only. `⟶ /upstream`

## 2026-07-14 — council-persistence (manual)
- [adr-council-persistence v1](2026-07-14-council-persistence/2026-07-14-adr-council-persistence-v1.md) — **Approved. ADR-007.** Records the *why* of making `/council` **persist the Council Record to the artifacts store a mandatory Phase-7 (Scribe) step**, shipped in `forgers-tech/skills` v1.7.0 ([PR #26](https://github.com/forgers-tech/skills/pull/26)). Gap fixed: the command rendered the record only into the chat, leaving a human to hand-copy it — but the whole forward-only lifecycle references the ledger *by path + stable ID* (`/upstream`/`/gatekeeper`/`/downstream`/`artifact-reconciliation` all take a `<Council Record path>`), so a transcript-only record breaks addressability and the mechanical lost/invented/drift detection the ledger exists for. Now the Scribe **discovers** the store (not hardcoded: sibling `*-artifacts/engineering/`, else `docs/councils/`), mirrors its dated-folder + versioned-file + frontmatter convention, updates `INDEX.md`, and reports paths under a new `## Persisted Artifacts` section; a transcript-only record is a **failed close** (Completion Guard); reopen writes `-v<N+1>` (never overwrites). Explicitly *not* the same as publishing the ADR/Security-Matrix/runbook a decision requires — that stays `artifact-reconciliation`'s job. Preserves the ADR-005 ledger + ADR-006 orchestration unchanged. Implementation lives in the `skills` repo; this ADR is rationale only. First persisted record: the Infisical secret-platform council (below)

## 2026-07-14 — infisical-secret-platform-council · Revision 2 (DEFERRED)
- [council-record v2 — DEFERRED](2026-07-14-infisical-secret-platform-council/2026-07-14-council-record-v2-deferred.md) — **Deferred / deprioritized.** Council reopened (§15) after a **direct check of infisical.com/pricing** refuted assumption A-4: the **free tier has no audit logs** (Pro-only, 90-day) and **caps at 5 identities** (machine+human) — but the design needs 5 machine identities alone (D-5), and audit (Q-1) is a confirmed requirement. Meeting the confirmed requirements needs **Pro ≈ $90–110/mo**, which the operator declines (no new financial commitment). Key insight: the real goal — *centralize env secrets in one place* — **SOPS+age already satisfies**; the gaps Infisical would close (read-audit + revocable least-privilege) are exactly the paid-tier features, real but not urgent enough to pay for now. **X-8: defer the whole initiative** — does NOT proceed to `/downstream`; D-1 deferred, D-2..D-13 parked; **P0.4 supersession withdrawn → SOPS+age stands as status quo**. Q-1/Q-2/Q-3 resolved. Resume triggers recorded (budget for Pro / compliance / team growth / cheaper equivalent). Self-host stays rejected (X-1, SPOF — unaffected by cost). Spec + GK review below are **parked, not deleted** (resume at `/downstream` if budget appears — spec is GK-approved 95/100)

## 2026-07-14 — infisical-secret-platform (/upstream) · PARKED
- [gatekeeper-review v1](2026-07-14-infisical-secret-platform/2026-07-14-gatekeeper-review-v1.md) — **🟡 Approved with Recommendations — 95/100.** Gatekeeper review of the Infisical Feature Spec. **Traceability Audit clean on all five checks** (D-1..D-13 all expanded, no invented requirements, no decision drift, Q-1/Q-2/Q-3 still surfaced, no revived X-#). No blocking issues — Phase 0 (local dev) + most of Phase 1 can start now. Scores: Product 25, Business Rules 22 (no dedicated edge-case list; idempotency implicit), Architecture 24, Tech Leadership 24 (no explicit PR-sequencing note). Important risks: **Q-2 (FinOps cost)** front-load before Cloud/operator spend (could reopen F-2); **Q-3 (operator last-known-good on outage)** is a hard **Phase-2** entry gate; **least-privilege (D-5)** only met if the per-app SA + resourceNames RBAC ships with adoption; svc+ingest shared-key cutover is the highest-blast-radius step. Next: `/downstream` (Phase 0/1)
- [feature-spec-infisical-secret-platform v1](2026-07-14-infisical-secret-platform/2026-07-14-feature-spec-infisical-secret-platform-v1.md) — **Draft. Ready for Gatekeeper.** Authors the Infisical secret-platform Council Record (below) into an implementation-ready spec — every section cites `⟶ D-#`, no re-discovery. Adopt **Infisical Cloud** via the **Kubernetes Operator** (`InfisicalSecret` CRD → native Secret, `creationPolicy: Orphan`, pinned), phased (P0 local-dev → P1 TF-double-write collapse + rotate workflow + operator pilot w/ outage test → P2 gated per-app runtime cutover, `web→bff→…→ingest+svc` last on a shared `QUEUE_ENCRYPTION_KEYS` path), with **per-workload identities + per-app ServiceAccount/`resourceNames` RBAC** (both boundaries — least-privilege isn't delivered by adoption alone), a **static Universal-Auth operator identity sealed in SOPS** (tailnet-only `6443` forecloses K8s-native auth), and **SOPS retained permanently** as bootstrap/DR break-glass. 10 BR-# (each `⟶ D-#`), no app code change (`envFrom.secretRef` seam), validation = decoded parity-diff + functional round-trip (zero `decrypt_failed`) + executed rollback drill; drift alerted outside `argocd diff`. Two gates surfaced as `TODO:` — **Q-2** (FinOps Cloud cost) blocks go-live, **Q-3** (empirically validate operator last-known-good on outage) blocks Phase 2. Estimate **GG**. Next: `/gatekeeper`

## 2026-07-14 — infisical-secret-platform-council (/council)
- [council-record v1](2026-07-14-infisical-secret-platform-council/2026-07-14-council-record-v1.md) — **Approved. Decision record (not a spec).** Critical-depth `/council` on adopting **Infisical** as Webhookr's secret platform. Verdict: **adopt Infisical Cloud**, scoped + phased, via the **Kubernetes Operator (InfisicalSecret CRD → native Secret, `creationPolicy: Orphan`, pinned)**, with **SOPS+age retained permanently** as bootstrap/DR anchor + break-glass — "single platform" is unachievable (bootstrap chicken-and-egg). Four conflicts resolved: F-1 adopt-vs-extend (centralized read-audit is **categorically unmeetable by SOPS** — the deciding bright line; Devil's Advocate conceded a 2nd age recipient is escrow, not least-privilege); F-2 **Cloud** not self-host (self-host = non-rebuildable SPOF per unleash-db precedent; egress already runs Neon/Redis-Cloud-class SaaS on 443); F-3 scope = **phased**, runtime cutover IN (local-dev → TF-double-write collapse + rotate workflow + operator pilot → gated per-app runtime, svc/ingest last on a shared `QUEUE_ENCRYPTION_KEYS` path); F-4 tailnet-only `6443` forecloses K8s-native TokenReview → **static Universal-Auth secret seeded via SOPS**. Challenges: operator leaves last-known-good Secret on outage (**CONFIRMED-by-source**, must test before P2 — A-1/Q-3); least-privilege only **PARTIALLY MET** without net-new per-app ServiceAccount + `resourceNames` RBAC + `automountServiceAccountToken:false` (D-5). Backend: **no app code change** (`envFrom.secretRef` seam unchanged). 10 US, 13 D-#, A/F/Q/R/X registers. **Supersedes audit P0.4** ("extend SOPS"); requires an ADR + Security Matrix row on implementation. Next: `/upstream`

## 2026-07-14 — council-multi-agent (manual)
- [adr-council-multi-agent v1](2026-07-14-council-multi-agent/2026-07-14-adr-council-multi-agent-v1.md) — **Approved. ADR-006.** Records the *why* of rewriting `/council` from a single model *simulating* a committee into a **multi-agent orchestrator**, shipped in `forgers-tech/skills` v1.6.0 ([PR #24](https://github.com/forgers-tech/skills/pull/24)). Prime directive "orchestrate, never simulate": the Chair authors no specialist output — every opinion/conflict/consensus comes from an actual independent execution (Claude Code Task tool, parallel & stateless Round 1); blocks if the runtime can't launch independent agents (no single-pass fallback). Phased protocol (Plan+Evidence → independent Round 1 → Consensus Matrix → conflict-resolution round → candidates → challenge round → decision → Scribe); coordination only through artifacts. Historian + Devil's Advocate promoted to permanent independent agents; contracts inline (portable, runtime-agnostic). **Preserves the ADR-005 ledger contract unchanged** — only *how* the Ledger is produced changes. Also added a best-effort, never-invented `Tokens` column to every Execution Trace. Implementation lives in the `skills` repo; this ADR is rationale only

## 2026-07-14 — decision-ledger-sdlc (manual)
- [adr-decision-ledger-sdlc v1](2026-07-14-decision-ledger-sdlc/2026-07-14-adr-decision-ledger-sdlc-v1.md) — **Approved. ADR-005.** Records the *why* of redesigning the feature pipeline from independent prompts into a **forward-only Decision-Ledger operating system**, shipped in `forgers-tech/skills` v1.4.0 ([PR #20](https://github.com/forgers-tech/skills/pull/20)) + execution-time traceability v1.5.0 ([PR #22](https://github.com/forgers-tech/skills/pull/22)). Root fault fixed: `/upstream` never consumed the Council Record, so it re-ran discovery/architecture `/council` had already decided → duplication + drift. Keystone: one **Decision Ledger** owned by `/council` (`D-#` each serving a `US-#`, plus `A-/Q-/R-/X-`), referenced never redefined by every later stage, making lost/invented/drifted decisions mechanically detectable. Five commands not six (User Story = card fed to `/council`, the hard 4-item admission gate); `/gatekeeper` now diffs spec vs Ledger (Traceability Audit); decision immutability enforced per-stage (reopen `/council`, never self-resolve); `artifact-reconciliation` gains a decision-survival audit. Plus an immutable-`Started` Execution Trace on every output. Implementation lives in the `skills` repo; this ADR is rationale only

## 2026-07-11 — processor-execution-timeline-council (/council, benchmark)
- [council-record v1](2026-07-11-processor-execution-timeline-council/2026-07-11-council-record-v1.md) — **Reference. Does not supersede the implemented spec (`…-v3`).** Independent `/council` benchmark reconstruction of the Processor Execution Timeline decision: reasoned from the user story + repo evidence with v3's feature-specific decisions quarantined, then compared. **Full convergence on the AC-scoped feature** — including the non-obvious crash-window idempotency insight (return the persisted outcome so a crash between persist and delivery-enqueue doesn't lose the webhook), independently rediscovered. **One material divergence:** scope philosophy — the council *defers* force-send/non-overridable/trigger to a scoped fast-follow (with recorded Product/Security dissent) and makes "gate must not go live without an escape hatch" an explicit release-ordering gate; v3 *bundled* force-send. Deciding input = author intent on OQ-2 (does force-send belong to this story?). Shipped code already matches v3; this is a documentation/scope note, not a code change

## 2026-07-10 — downstream-command (manual)
- [adr-downstream-command v1](2026-07-10-downstream-command/2026-07-10-adr-downstream-command-v1.md) — **Approved.** Adds `/downstream` as the implementation stage of the feature pipeline (`/upstream` spec → `/gatekeeper` review → `/downstream` implement), shipped in `forgers-tech/skills` v1.2.0 (PR #18). Records the *why*: close the pipeline so the upstream quality gate isn't lost at build time, encode the org's engineering standards once (evidence-first, repository-first, no-hallucination, security/observability-by-default, tests, docs, safe rollout) as the executable counterpart to `artifact-reconciliation`, and cut operator variance via a consistent implementation report. Multi-perspective (not single-persona); halts on missing mandatory inputs. Implementation lives in the `skills` repo; this ADR is the rationale only

## 2026-07-10 — processor-execution-timeline (/upstream → /downstream)
- [feature-spec-processor-execution-timeline v3](2026-07-10-processor-execution-timeline/2026-07-10-feature-spec-processor-execution-timeline-v3.md) — **current. Implemented + merged 2026-07-11.** As-built; supersedes v2 with two Copilot-review refinements: Redis in-flight lock TTL **~300s not ~60s** (crash cap must exceed the destination-timeout fan-out) and **force send leaves the event untouched** (no `lastReplayedAt` bump — a forced delivery isn't a replay; audited via the FORCED delivery + log + metric). Otherwise identical to the 98/100-approved v2
- [implementation-notes v2](2026-07-10-processor-execution-timeline/2026-07-10-implementation-notes-v2.md) — **current. As-built, merged + deployed dark 2026-07-11.** Supersedes v1 (which was a PR-open snapshot). Deploy path = migrate initContainer → build-deploy gitops bump → ArgoCD (all 4 app deploys green); Grafana dashboard applied live via `make`. Review refinements folded into spec v3 (lock TTL ~300s; force send leaves event untouched); AF-07 force-on-passed + `TIMESTAMP(3)` kept with justification; infra security matrix needed no entry (no new port/egress). Remaining: staged flag rollout, mobile fast-follow, Grafana alerts, `overridable` UI hint
- [implementation-notes v1](2026-07-10-processor-execution-timeline/2026-07-10-implementation-notes-v1.md) — superseded by v2. PR-open snapshot: as-built record + build-time decisions/why before review & merge As-built record across svc [#130](https://github.com/forgers-tech/webhookr-svc/pull/130), bff [#77](https://github.com/forgers-tech/webhookr-bff/pull/77), web [#88](https://github.com/forgers-tech/webhookr-web/pull/88), docs [#12](https://github.com/forgers-tech/webhookr-docs/pull/12). Build-time decisions + *why*: `overridable` kept off the GraphQL contract (server-side 409, initial set empty); bff 403→Forbidden mapping for the force-send kill-switch; Redis lock as a self-owned ioredis client (`DeliveryLockService`) per repo convention; `trigger` threaded through the delivery list for the Forced badge; web adoption metric as a `webhookr:analytics` CustomEvent (no sink yet); dedicated `force-deliver` controller. Verified: svc 693u/130e2e, bff 346, web 236, docs build. Follow-ups: mobile, Grafana panels, `overridable` UI hint
- [feature-spec-processor-execution-timeline v2](2026-07-10-processor-execution-timeline/2026-07-10-feature-spec-processor-execution-timeline-v2.md) — superseded by v3. **Reviewed — Gatekeeper 98/100 ✅ Approved.** v1 + all review findings: idempotency short-circuit **returns the persisted outcome** (no lost delivery on crash-between-persist-and-enqueue, IR-1); `haltedBy` derived from the timeline for the non-overridable check (IR-2); **Redis in-flight lock** for force/replay dedup (IR-3); `processingStatus` exposed on the read contract; **non-overridable processors** (`overridable` code-prop → 409, BR-013; initial set none); `trigger` enum over `forced` boolean; "required processor" abstraction removed; reason-code catalog + force-send UX copy in-spec; **three toggles** in [[webhookr-toggles]] (`web/event-timeline`, `svc/force-send` kill-switch, `web/force-send` button-gate). Ready for implementation. Estimate M
- [feature-spec-processor-execution-timeline v1](2026-07-10-processor-execution-timeline/2026-07-10-feature-spec-processor-execution-timeline-v1.md) — superseded by v2. Original Draft: expose the per-event processor timeline (gap-closure) — `EventProcessorRun.startedAt/endedAt`, write-once immutable persist, ordered `processorRuns` across svc→bff→web, force send (`Delivery.trigger` + `forceDeliverEvent`), Grafana panel; OQ-1..5 + IR-1/2/3 + R-2 resolved during review

## 2026-07-06 — processor-config-management (/upstream)
- [implementation-notes v3](2026-07-06-processor-config-management/2026-07-06-implementation-notes-v3.md) — **current.** v2 + refined deviation #4: **project** processors moved from the global-Settings tab to a **new per-project page** `/dashboard/projects/[id]` (Overview/Processors tabs, mirroring the endpoint page); project name in the list links to it; Settings tab removed (webhookr-web#87, deployed `0a6b67d`). Now each scope configures on its own screen
- [implementation-notes v2](2026-07-06-processor-config-management/2026-07-06-implementation-notes-v2.md) — superseded by v3. v1 + updated deviation #4: the web nav surface is now **configuration tabs** (endpoint detail = Overview/Processors; project = a Processors tab on Settings, scoped to the selected project) instead of the standalone unlinked route, which was removed (webhookr-web#86, deployed `5798188`); adds the guard-naming clarification (global `CompositeAuthGuard`) and deploy record (svc `4e365c0`, bff `d4f59d4`, web `5798188`)
- [implementation-notes v1](2026-07-06-processor-config-management/2026-07-06-implementation-notes-v1.md) — superseded by v2. As-built record: shipped across webhookr-svc (`src/processor-config/`, ADR 0004 + docs), webhookr-bff (`src/processor-configs/` GraphQL + `put` on the client), webhookr-web (`features/processor-configs` + project route + endpoint-detail section); no migration (tables pre-existed); deviations: standard Nest exceptions instead of custom `error` codes (status codes match spec), rejection metric emits 3 reasons (not `not_owned`), project UI is On/Off toggle only, Playwright e2e deferred
- [feature-spec-processor-config-management v2](2026-07-06-processor-config-management/2026-07-06-feature-spec-processor-config-management-v2.md) — **current.** v1 + Gatekeeper findings (91/100): read model now applies the **global** `webhookr.processors.enabled` flag (`engineEnabled`/`source=engine_disabled`; resolver alone doesn't — engine BR-009) resolving IR-1; **writes allowed while engine globally off** (persist intent, BR-C10); catalog roadmap-exposure of not-yet-GA processors accepted as explicit tradeoff (R-C6, IR-2); BFF `ProcessorConfig.id` for Apollo normalization (IR-3); minors (error-flow numbering, dropped invented `processorName` regex, `config` 400, `unavailable` rejection metric, removed stale UI flag)
- [feature-spec-processor-config-management v1](2026-07-06-processor-config-management/2026-07-06-feature-spec-processor-config-management-v1.md) — superseded by v2. Write surface for the engine story's deferred config: catalog + project/endpoint CRUD to enable/disable **PRODUCT** processors across `webhookr-svc` (REST, `JwtAuthGuard` + ownership mirroring `EventService.findOne`), `webhookr-bff` (GraphQL), `webhookr-web` (project toggle / endpoint tri-state On-Off-Inherit). Writes only the two customer layers of BR-003; reuses `PipelineResolver` (event-free) for the effective-state read model; catalog lists **all** PRODUCT processors; PLATFORM→409, unknown→404, platform-off PUT→409 `ProcessorUnavailable`; `config Json` reserved; **no migration** (tables already shipped), forward-only, engine/resolver/delivery untouched. Estimate G (M if project-scope only)

## 2026-07-06 — event-processor-engine (/upstream)
- [implementation-notes v1](2026-07-06-event-processor-engine/2026-07-06-implementation-notes-v1.md) — as-built record: shipped in webhookr-svc#127; one deviation from spec v2 (`processorName` dropped from `EventProcessorResult`, redundant with the registry-known `processor.name`); code-side records are ADR 0003 + docs/event-processing.md; histogram-bucket convention codified in svc CLAUDE.md
- [feature-spec-event-processor-engine v2](2026-07-06-event-processor-engine/2026-07-06-feature-spec-event-processor-engine-v2.md) — **current.** v1 + Gatekeeper findings: replay gate (BR-015: reject replay of `STOPPED`/`FAILED`, keep `PENDING`/`PROCESSED` replayable), explicit event state machine + terminality, two-write transaction boundary + crash recovery (BR-017), config-read perf budget/cache trigger
- [feature-spec-event-processor-engine v1](2026-07-06-event-processor-engine/2026-07-06-feature-spec-event-processor-engine-v1.md) — superseded by v2. Upstream feature spec for the pre-delivery processor pipeline in `webhookr-svc`: orchestration-only engine + `PipelineResolver` (4-layer enablement: global flag ∧ per-processor flag ∧ project ∧ endpoint, endpoint>project>platform default), PLATFORM vs PRODUCT categories, `Event.processingStatus` split from `EventProcessorRun` status (+DISABLED), delivery gated on `passed`, config tables schema+read-path (write surface deferred); reviewed 95/100 Approved-with-Recommendations

## 2026-07-05 — infra-security-matrix (manual)
- [adr-infra-security-matrix v1](2026-07-05-infra-security-matrix/2026-07-05-adr-infra-security-matrix-v1.md) — network least-privilege: deny-by-default egress (port-scoped, private-range except = SSRF guard), delivery 443-only, /docs blocked in depth; accepted risks (Neon no IP allowlist → GA revisit, 41641/ICMP, BFF public); operational matrix in gitops
- [threat-model-infra v1](2026-07-05-infra-security-matrix/2026-07-05-threat-model-infra-v1.md) — STRIDE-lite over the infra trust boundaries (SSRF, exfiltration, lateral movement, recon, origin bypass, admin exposure, supply chain) with the mitigating control per threat; residual/accepted risks

## 2026-07-05 — redis-cloud-migration (manual)
- [adr-redis-cloud-migration v1](2026-07-05-redis-cloud-migration/2026-07-05-adr-redis-cloud-migration-v1.md) — replace in-cluster K3S Redis (audit P0: SPOF, no backup) with Redis Cloud HA + AOF 1s + TLS; single `rediss://` `REDIS_URL` across ingest/svc/bff; Terraform-imported; big-bang cutover

## 2026-07-05 — ingest-slug-validation (manual)
- [adr-ingest-slug-validation v1](2026-07-05-ingest-slug-validation/2026-07-05-adr-ingest-slug-validation-v1.md) — validate slug at the edge via authenticated internal resolver in svc + in-process cache + opossum breaker + stale-while-error; `whr_` + 16 base62 slugs; 5 options compared

## 2026-07-01 — webhookr-architecture-audit (manual)
- [audit-report v1](2026-07-01-webhookr-architecture-audit/2026-07-01-audit-report-v1.md) — full prioritized audit (P0–P3): supply-chain PAT, Redis SPOF, unprovisioned alerts, secret hygiene; refuted false positives; remediation plan
